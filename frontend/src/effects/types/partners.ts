export interface Partner {
  description: string;
  done: boolean;
  id: number;
  logo: string | null;
  name: string;
  partner_id: number;
  priority: number;
  rewardParams: string;
  rewardType: string;
  url: string;
}
